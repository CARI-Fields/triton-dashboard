// The board uses ONE shared team login, so people only ever type a password.
// This email is the fixed account you create in Supabase (Authentication ->
// Users -> Add user, with "Auto Confirm User" checked). The password you set
// there is the team password.
export const TEAM_EMAIL = "team@triton-board.app";
